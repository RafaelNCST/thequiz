import React from 'react';
import { ContainerMenu, TextMenu} from '../MenuInicialTheQuiz/styled';

export const InicioMenuTheQuiz = () => {
  return (
    <ContainerMenu>
      <TextMenu>Início</TextMenu>
    </ContainerMenu>
  )
}